Socket fucntion: int sockfd = socket(int Domain(IPV4),int type(Scoke_stream(TCP),Int Protocl(0 because its the default of the TCP protocol)))

Bind function: int bind(int sockfd, const stuct sockaddr *addr, socklen_t addrlen);

Listing fucntion: int listen(int sockfd,int backlog(How mnay connection would be accepted making a limit));

Accept Fucntion: newsockfd = accept(sockfd,(struct sockaddr*) &addr,&addrlen);

Conntection Fucntion: int connect(int sockfd, const struct sockaddr *addr,socklen_t addrlen);
0 = Success conection 
-1 = failure 

Reading fucntion = int read(newsockfd,buffer(string),buffer_size(string size));

Writing Function: int write(newsockfd,buffer,buffer_size);

To awake server on server file do 
./Server 9898 when in the file Socketing

On to start the communication on clinet
./Client 127.0.0.1 9898